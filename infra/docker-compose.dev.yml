# Local development database only. The full stack lives in docker-compose.yml (later).
# Usage: docker compose -f infra/docker-compose.dev.yml up -d
services:
  postgres:
    image: postgres:18.4
    container_name: wishly-postgres-dev
    environment:
      POSTGRES_USER: wishly
      POSTGRES_PASSWORD: wishly
      POSTGRES_DB: wishly
    ports:
      - "5432:5432"
    volumes:
      # Postgres 18 keeps PGDATA in a versioned subdirectory; mounting the parent
      # is what the official image recommends so pg_upgrade --link stays possible.
      - wishly_pgdata:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wishly -d wishly"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped

volumes:
  wishly_pgdata:
    name: wishly_pgdata
