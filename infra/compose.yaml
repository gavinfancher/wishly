# Wishly application stack.
#
#   docker compose -f infra/compose.yaml --env-file infra/.env up -d
#
# Three long-lived containers. The database is NOT here: it is PlanetScale
# Postgres, reached over the public internet via DATABASE_URL. Apply the schema
# by hand with psql; there are no migrations.
#
# CONFIGURATION. Nothing in this file carries application config. The api and
# worker entrypoints load it from the AWS Secrets Manager secret named by
# WISHLY_SECRETS_ID, so `infra/.env` holds only what is needed to reach AWS:
# that name and an IAM user's keys.
#
# Build context is the repo root (..) so the images can copy backend/.
# No application port is published — inbound traffic arrives via cloudflared.
services:
  api:
    build:
      context: ..
      dockerfile: infra/Dockerfile.api
    image: wishly-api:latest
    env_file:
      - .env
    # Which secret the entrypoint loads. Unset it and the entrypoint does
    # nothing, leaving .env as the whole configuration. The credentials boto3
    # uses arrive via env_file above; the region is fixed in the code.
    environment:
      WISHLY_SECRETS_ID: ${WISHLY_SECRETS_ID:-}
    healthcheck:
      test:
        - CMD-SHELL
        - python -c "import urllib.request;urllib.request.urlopen('http://localhost:8000/health')"
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s
    restart: unless-stopped
    networks:
      - app_net

  # The send pipeline. `python -m wishly.orchestration.flows` calls Prefect's
  # serve(), which registers both deployments with their schedules and then runs
  # their flow runs in subprocesses of this container. One process does the whole
  # job — there is no work pool, no separate worker, and no prefect.yaml.
  #
  # Scheduling lives in Prefect Cloud (PREFECT_API_URL + PREFECT_API_KEY), so the
  # control plane survives this host going away — which is the whole point of
  # keeping the scheduler off the machine it schedules.
  worker:
    build:
      context: ..
      dockerfile: infra/Dockerfile.worker
    image: wishly-worker:latest
    env_file:
      - .env
    environment:
      WISHLY_SECRETS_ID: ${WISHLY_SECRETS_ID:-}
    restart: unless-stopped
    networks:
      - app_net

  cloudflared:
    image: cloudflare/cloudflared:2026.8.3
    command: tunnel --no-autoupdate --metrics 0.0.0.0:2000 run
    environment:
      TUNNEL_TOKEN: ${TUNNEL_TOKEN}
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "cloudflared", "--version"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    networks:
      - app_net

networks:
  app_net:
    driver: bridge
